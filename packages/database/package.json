{
  "name": "database",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:prisma:generate": "prisma generate",
    "db:migrate:local": "dotenv -e .env.local -- prisma migrate dev",
    "db:reset:local": "dotenv -e .env.local -- prisma migrate reset",
    "db:studio:local": "dotenv -e .env.local -- prisma studio",
    "db:container:local": "docker compose up -d",
    "db:migrate:remote": "dotenv -e .env.remote -- prisma migrate deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.10.0",
  "dependencies": {
    "@prisma/client": "^6.16.3",
    "prisma": "^6.16.3"
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0"
  }
}
